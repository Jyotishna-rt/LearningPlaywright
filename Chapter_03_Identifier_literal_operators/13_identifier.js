/*

let class = "Hello World";

//class is a reserved keyword in js, so we cannot use it as an identifier,
//but we can use it as an identifier if we use it as a property of an object

let if = true;
//if is also a reserved keyword in js, so we cannot use it as an identifier,
//but we can use it as an identifier if we use it as a property of an object

let return = 6;
//return is also reserved keyword in js, so we cannot use it as an identifier,
//but we can use it as an identifier if we use it as a property of an object

let undefined = 5;//works but never do this
let infinity = 10; //workds but never do this
//undefined and infinity are also reserved keywords in js, so we cannot use them as identifiers,
//but we can use them as identifiers if we use them as properties of an object




*/